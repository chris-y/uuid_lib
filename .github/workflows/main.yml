on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: build
    steps:
      - uses: actions/checkout@v1
      - name: ppc build
        id: build
        uses: chris-y/amigaos-cross-docker-action@HEAD
        with:
          makefile: 'uuid_files/Makefile'
